---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 'latest'


inputs:
  - name: controller-go
  
run:
  path: sh
  args:
  - -exc
  - | 
    pwd
    ls -ltr 
    ls -ltr ..
    ls -ltr ../..
    ls -ltr build/
    make
