---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: alpine
    tag: 'latest'

run:
  path: sh
  args:
  - -exc
  - |
    whoami
    env
    go version
    find .
    make
