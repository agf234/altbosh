---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 'latest'

run:
  path: sh
  args:
  - -exc
  - |
    whoami
    env
    go version
    find .
    make
